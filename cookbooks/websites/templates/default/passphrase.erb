# <%= node['config_disclaimer'] %>

# Configuration for the basic passphrase web toy I made

etag on;

root /var/wwww/passphrase;

location /passphrase {
  index index.html;
  autoindex off;
  expires 21d;
}

location /passphrase/js {
  expires max;
}

location /passphrase/css {
  expires max;
}

