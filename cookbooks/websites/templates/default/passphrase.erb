# <%= node['config_disclaimer'] %>

# Configuration for the basic passphrase web toy I made

etag on;

location /passphrase {
  alias /var/www/passphrase_toy/;
  index index.html;
  autoindex off;
  expires max;
}
